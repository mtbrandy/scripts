Simple script to clone an existing OpenJDK git repository and build it.

```
usage: get_openjdk.sh <git-repo> <tag-or-branch>
```

For example,

```
$ get_openjdk.sh git@github.com:AdoptOpenJDK/openjdk-jdk8u.git dev
```
